# Service routes traffic to pods labelled with `active: "true"`.
apiVersion: v1
kind: Service
metadata:
  name: my-app
  labels:
    app: my-app
spec:
  type: ClusterIP
  selector:
    app: my-app
    active: "true"
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
