apiVersion: v1
kind: Service
metadata:
  name: scrum-service
spec:
  type: NodePort
  selector:
    app: scrum-app
    version: blue
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30010
