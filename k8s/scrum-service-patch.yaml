apiVersion: v1
kind: Service
metadata:
  name: scrum-service
  namespace: softer-eng
spec:
  selector:
    app: scrum-app
    version: green
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30010
  type: NodePort
